def readFile(filename)
  read file
  loop through file
    read patients as patient object
  return list of patient objects

def readDatabase
  create queries
  loop through patients
    query database
    store patients as patient object
  return list of patient objects

def printDatabase
  readDatabase
  loop through patients
    print patients to standard out
